import '../styles/header.scss'

const Header = () => {
    return (
        <header className="header">
            <div>
                <h1>Dashboard</h1>
            </div>
        </header>
    );
}

export default Header;